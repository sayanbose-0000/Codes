package com.org;

// External API, not yet implemented, but used in MyService
// Inspite of not being implemented, we want to test it in our MyService
public interface ExternalAPI {
    public String getData();
}
