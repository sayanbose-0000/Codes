const os = require('os');

//amount of free memory available
console.log(os.freemem());
//shows home directory
console.log(os.homedir());
//shows host hostname
console.log(os.hostname());
//shows platform we are working on
console.log(os.platform());
//version
console.log(os.release());
//os type
console.log(os.type());